# show dependencies #
cd catkin_ws
rospack depends my_cloud_fusion

# build workspace #
cd 
cd catkin_ws
catkin_make

# start bagfiles
roslaunch my_cloud_merger bag.launc

# launch fusion node
roslaunch my_cloud_fusion fusion_node.launch

# add transformation
rosrun tf2_ros static_transform_publisher 0 0 0 0 0 0 1 base_footprint points_fused


# searched tf's #
/base link for tf /frame_id               / topic
/base_footprint   /livox_front            /livoxfront/livox/lidar
/base_footprint   /velodyne_front_left    /velodyne/front_left/velodyne_points
/base_footprint   /velodyne_front_right   /velodyne/front_right/velodyne_points-

## git ##

    # status
    cd catkin_ws/src
    git status 

    # git add
    git add <file_1>

    # git commit
    git commit -m "message"

    # git push
